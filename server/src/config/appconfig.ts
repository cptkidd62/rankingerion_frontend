export type AppConfig = {
  usersFile: string;
  botsFile: string;
  matchesFile: string;
  dataDir: string;
  botsDir: string;
  botName: string;

  useBenchmarker: boolean;

  autoSave: boolean;
};
